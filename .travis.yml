language: python
python:
  - "3.6"
  - "3.6-dev"  # 3.6 development branch

sudo: required

sevices:
  - docker

install:
  - pip install -r requirements.txt
  - pip install codecov
  - pip install coverage

script:
  - nosetests -vse --with-coverage tests/

after_success:
  - codecov
